<% include ../partials/header %>
    <div class="row">
        <div class="login-form">
            <h2><%= playground.name %> bearbeiten</h2>
            <form action="/playgrounds/<%= playground._id %>?_method=PUT" method="POST">
                <div class="form-group">
                    <input class="form-control" type="text" name="playground[name]" value="<%= playground.name %>">
                </div>
                <div class="form-group">
                    <input class="form-control" type="text" name="playground[image]" value="<%= playground.image %>">
                </div>
                <div class="form-group">
                    <input class="form-control" type="text" name="playground[description]" value="<%= playground.description %>">
                </div>
                <div class="form-group">
                    <input class="form-control" type="text" name="location" id="location" value="<%= playground.location %>">
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-primary btn-block">Speichern!</button>
                </div>
            </form>
            <a href="/playgrounds">Zurück</a>
        </div>
    </div>
<% include ../partials/footer %>
